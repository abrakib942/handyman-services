export enum tagTypes {
  user = "user",
  service = "service",
  workType = "workType",
  booking = "booking",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.service,
  tagTypes.workType,
  tagTypes.booking,
];
